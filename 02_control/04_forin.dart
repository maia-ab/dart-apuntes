main() {
  List<String> listado = ["Batman", "Superman", "Mujer Maravilla"];

  for (String nombre in listado) {
    // Ejecuta el codigo las mismas veces que la cantidad de elemento que haya en la lista.
    print(nombre);
  }
}
